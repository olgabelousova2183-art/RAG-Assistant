# RAG Ассистент с OpenAI API

Готовый RAG (Retrieval Augmented Generation) ассистент, подключенный к OpenAI API для создания умного помощника с базой знаний.

## 🚀 Что нужно для создания RAG-ассистента

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка OpenAI API

1. Получите API ключ на [platform.openai.com](https://platform.openai.com/api-keys)
2. Создайте файл `.env` в корне проекта:

```env
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4-turbo-preview
EMBEDDING_MODEL=text-embedding-3-small
PERSIST_DIRECTORY=./chroma_db
```

### 3. Подготовка документов

Поместите ваши PDF файлы в корневую директорию проекта. По умолчанию система ищет файл `База_знаний_НейроСфера.pdf`.

## 📖 Использование

### Базовое использование

```python
from rag_assistant import RAGAssistant

# Инициализация
assistant = RAGAssistant()

# Загрузка документов
documents = assistant.load_documents(["База_знаний_НейроСфера.pdf"])

# Создание векторного хранилища
assistant.create_vectorstore(documents)

# Настройка цепочки вопрос-ответ
assistant.setup_qa_chain(k=4)

# Задать вопрос
result = assistant.ask("Что такое нейросфера?")
print(result['result'])
```

### Запуск интерактивного режима

```bash
python rag_assistant.py
```

Это запустит интерактивную консоль, где вы можете задавать вопросы ассистенту.

## 🔧 Компоненты системы

### 1. **Загрузка документов** (`load_documents`)
- Поддержка PDF файлов
- Автоматическое извлечение текста

### 2. **Векторное хранилище** (`create_vectorstore`)
- Разбиение документов на чанки
- Создание embeddings с помощью OpenAI
- Сохранение в ChromaDB для быстрого поиска

### 3. **Retrieval** (Извлечение)
- Поиск релевантных фрагментов по запросу
- Настраиваемое количество возвращаемых документов (k)

### 4. **Generation** (Генерация)
- Использование GPT-4 для генерации ответов
- Контекст из базы знаний + вопрос пользователя

## ⚙️ Настройки

### Параметры инициализации

```python
assistant = RAGAssistant(
    openai_api_key="your-key",      # API ключ (опционально, можно в .env)
    model_name="gpt-4-turbo-preview",  # Модель для генерации
    embedding_model="text-embedding-3-small",  # Модель для embeddings
    persist_directory="./chroma_db"  # Директория для векторной БД
)
```

### Параметры чанкинга

```python
assistant.create_vectorstore(
    documents,
    chunk_size=1000,      # Размер чанка в символах
    chunk_overlap=200     # Перекрытие между чанками
)
```

### Параметры поиска

```python
assistant.setup_qa_chain(k=4)  # Количество релевантных документов
```

## 📁 Структура проекта

```
RAG/
├── rag_assistant.py          # Основной скрипт RAG ассистента
├── requirements.txt          # Зависимости Python
├── .env                      # Конфигурация (создайте сами)
├── chroma_db/                # Векторное хранилище (создается автоматически)
├── База_знаний_НейроСфера.pdf # Ваши документы
└── README.md                 # Документация
```

## 🔑 Ключевые особенности

- ✅ Автоматическая обработка PDF документов
- ✅ Персистентное хранение векторной БД (не нужно пересоздавать каждый раз)
- ✅ Интерактивный режим для удобного общения
- ✅ Настраиваемые параметры (модели, размер чанков, количество документов)
- ✅ Подробные логи процесса работы

## 💡 Примеры использования

### Пример 1: Быстрый вопрос

```python
assistant = RAGAssistant()
assistant.load_existing_vectorstore()
assistant.setup_qa_chain()
result = assistant.ask("Какие основные темы в документе?")
```

### Пример 2: Загрузка нескольких документов

```python
assistant = RAGAssistant()
documents = assistant.load_documents([
    "документ1.pdf",
    "документ2.pdf",
    "документ3.pdf"
])
assistant.create_vectorstore(documents)
assistant.setup_qa_chain()
```

## 🛠️ Устранение проблем

### Ошибка: "OPENAI_API_KEY не найден"
- Убедитесь, что создали файл `.env` с вашим API ключом
- Проверьте, что ключ указан правильно

### Ошибка: "Векторное хранилище не найдено"
- Сначала загрузите документы и создайте хранилище
- Или укажите правильный путь в `persist_directory`

### Медленная работа
- Используйте более быструю модель для embeddings (`text-embedding-3-small`)
- Уменьшите `chunk_size` для меньших чанков
- Уменьшите `k` для меньшего количества извлекаемых документов

## 📝 Лицензия

Этот проект создан для образовательных целей.

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы, проверьте:
1. Правильность API ключа OpenAI
2. Установлены ли все зависимости
3. Существуют ли загружаемые файлы

