# 🚀 Быстрый старт

## Шаг 1: Установка зависимостей

```bash
pip install -r requirements.txt
```

## Шаг 2: Настройка OpenAI API

1. Получите API ключ: https://platform.openai.com/api-keys
2. Создайте файл `.env` в корне проекта:

```env
OPENAI_API_KEY=sk-your-key-here
```

## Шаг 3: Запуск

### Вариант 1: Интерактивный режим (рекомендуется)

```bash
python rag_assistant.py
```

Система автоматически:
- Загрузит PDF файл `База_знаний_НейроСфера.pdf`
- Создаст векторное хранилище (если его еще нет)
- Запустит интерактивную консоль для вопросов

### Вариант 2: Использование в коде

```python
from rag_assistant import RAGAssistant

assistant = RAGAssistant()
documents = assistant.load_documents(["База_знаний_НейроСфера.pdf"])
assistant.create_vectorstore(documents)
assistant.setup_qa_chain()
result = assistant.ask("Ваш вопрос здесь")
print(result['result'])
```

## ✅ Готово!

Теперь вы можете задавать вопросы ассистенту на основе вашей базы знаний.

---

**Примечание:** При первом запуске создание векторного хранилища может занять несколько минут в зависимости от размера документов.

